# 어린이집 살림도우미 — 개발 목적과 개발 과정

> 이 문서는 **디자인 작업자에게 건네는 인수인계 문서**입니다.
> 이 프로그램이 무엇을 왜 이렇게 만들었는지, 그리고 화면을 손볼 때
> **무엇은 바꿔도 되고 무엇은 바꾸면 안 되는지**를 적었습니다.
>
> 버전 v1.0.0 · 제작 이현재 · 의견 주실 곳 hjlee9446@korea.kr

---

## 1. 무엇을 만들었나

어린이집 실무자가 쓰는 데스크톱 프로그램 두 개를 한 파일로 묶은 것입니다.

| 프로그램 | 하는 일 |
|---|---|
| **기타필요경비 정산** | 보호자에게 받은 필요경비와 실제 쓴 돈을 **원아별로** 맞춰, 정산서·안내문·보고서·대장을 뽑습니다 |
| **물품관리** | 보유 물품을 등록하고 물품대장·재물조사 조서를 뽑습니다 (먼저 만들어 둔 완성품) |

**「어린이집 살림도우미」** 파일 **하나**를 어린이집에 건네면, 더블클릭해서 바로 씁니다.
설치도, 인터넷도, 서버도 없습니다.

---

## 2. 왜 만들었나 — 실무에서 무너지던 지점

필요경비는 **받을 때는 원아별로 받고, 쓸 때는 한 건으로 씁니다.**
지출결의서 한 장에 「전세버스 2대 1,320,000원」이라고 적히면,
그 돈이 **어느 아이 몫에서 얼마씩 빠졌는지는 어디에도 남지 않습니다.**

| 단계 | 지금까지 | 무엇이 사라지나 |
|---|---|---|
| 지출결의서 | 「전세버스 2대」 총액 1건 | 금액은 남음 |
| 정산서 적요란 | `차량대여2대(70/1,320,000)` | **인원수만 남고 누가 탔는지는 안 남음** |
| 개인별 지출대장 | 김서우 4월 10,500원 | **어느 지출에서 나온 돈인지 역추적 불가** |
| 담당자 교체 | 계산 근거 소실 | **그때부터 대장이 비기 시작** |

그래서 연말에 반환할 금액을 계산할 때가 되면,
근거가 없어 **처음부터 다시 세는 일**이 벌어집니다.

**이 프로그램의 해답은 하나입니다 — 인원수 대신 명단을 통째로 저장한다.**

지출 1건을 등록할 때 대상 원아를 체크하면, 그 **명단이 그대로 저장**됩니다.
담당자가 바뀌어도 근거가 남고, 개인별 대장의 금액을 누르면
**그 지출결의서 건과 그때의 참여 명단이 그대로 다시 뜹니다.**

> 이 프로그램은 감사 검증 도구가 아니라, 어린이집이 정산을 **쉽게 하도록 돕는 도구**입니다.
> 「한도액을 넘었나 안 넘었나」를 따지지 않습니다. 들어온 돈을 제대로 정산해 주는 것이 전부입니다.

---

## 3. 제도적 배경 (숫자가 이렇게 나오는 이유)

| 근거 | 이 프로그램에 반영된 것 |
|---|---|
| 「사회복지법인 및 사회복지시설 재무·회계 규칙」 제15조 | **세목 간 상계 금지** — 모든 계산은 세목별로 따로. 현장학습비가 남았다고 행사비 초과분을 메우지 않습니다 |
| 회계연도 | **3월 1일 ~ 익년 2월 말일** |
| 보육사업안내 | 세목 기본값 8개(입학준비금·특별활동비·현장학습비·차량운행비·행사비·앨범비·급식비·기타 시·도 특성화비용) |
| 계정과목 | 특별활동비 = **211목 수입 / 411목 지출**, 기타필요경비 = **221목 수입 / 421목 지출** |
| 위탁보육 | 위탁보육료를 받는 **달**의 지출은 위탁보육료로 충당 → 보호자 정산에서 제외 |

---

## 4. 설계 원칙 여섯 가지

이 여섯 가지가 화면 구조를 결정했습니다. **디자인을 바꿔도 이건 유지돼야 합니다.**

**① 명단을 저장한다 (인원수가 아니라)**
지출 1건 → 대상 원아 명단 → 1/n 배분. 명단이 근거입니다.

**② 나눠떨어지지 않아도 합계는 정확히 맞는다**
`몫 = floor(총액 ÷ 인원)`, 나머지는 **앞에서부터 1원씩** 더합니다.
배분 합계가 총액과 1원이라도 다르면 **저장이 막힙니다.**
(실제 어린이집 대장에 6,244원 / 6,243원이 섞여 있는 것이 이 계산입니다.)

**③ 세목끼리 섞지 않는다**
모든 잔액·정산은 세목별로 따로 계산합니다. 합계는 보여만 줍니다.

**④ 반은 통계 단위가 아니다**
반은 **원아를 빠르게 고르기 위한 도구**일 뿐입니다.
그래서 이 프로그램에는 **반별 통계가 없습니다.** 모든 숫자는 원아에게 붙습니다.

**⑤ 정산서까지만 만든다**
반환할 금액을 계산해 **정산서를 뽑는 데까지**가 이 프로그램의 일입니다.
실제 계좌 이체·반납결의·회계 전표는 어린이집이 합니다. 반환 기록 기능은 일부러 넣지 않았습니다.

**⑥ 자료는 이 컴퓨터 밖으로 나가지 않는다**
아동 이름·금액이 서버로 나가면 안 되므로 **브라우저 localStorage + JSON 백업**만 씁니다.
그래서 백업이 유일한 보험이고, 일주일 넘으면 화면 위에 알립니다.

---

## 5. 화면 구성

### 5-1. 맨 처음 화면 (`build.py` 안에 있음)

```
       [ 어린이집 이름 입력칸 ]        ← 두 프로그램이 함께 쓰는 이름. 여기서만 정한다
        어린이집 살림도우미
        쓰실 프로그램을 고르세요.

    [💰 기타필요경비 정산]  [📦 물품관리]

      자료 저장 위치 안내 · 백업 당부
   ─────────────────────────────
   버전 · 제작자 · 의견 주실 메일 주소
```

### 5-2. 기타필요경비 정산 — 탭 8개

| 탭 | 하는 일 |
|---|---|
| **기초설정** | 반 구성 · 세목 · 원아 명부 · 고정 명단 · 회계연도 마감과 승계 |
| **수납 입력** | **수납결의서 1건** 단위로 입력 (실제 서식과 같은 단위) |
| **지출 등록** | 지출결의서 1건 + 대상 원아 체크 → 자동 배분. **대장을 채우는 유일한 지점** |
| **원아별 현황** | 반 → 원아를 골라 세목별 수납·사용·남은 금액. 금액을 누르면 역추적 팝업 |
| **정산·보고** | 아래 네 갈래 |
| **대장** | 개인별 원아 수입대장 / 지출대장 (아동 × 계정과목 × 월 12칸) |
| **검증** | 저장 차단급 오류(bad) · 주의(warn) · 기록(info) |
| **사용설명서** | 상세본 / 빠르게본 / 상황별 FAQ 검색 |

**정산·보고 네 갈래**

| 갈래 | 받아보는 사람 | 내용 |
|---|---|---|
| 정산보고서 | **운영위원회** | **세목별 통계** (원아 개인별 아님) |
| 보호자용 개별 안내문 | 보호자 1명 | 그 아이의 수납·사용·남은 금액 + 지출건별 내역 |
| 반환 정산서 (전 원아) | 어린이집 내부 | 전 원아 × 세목 표. 2월말에 한 번 뽑는 것 |
| 중간퇴소·중간전환 정산 | 퇴소하는 보호자 | 기준일까지 계산 + 애매한 지출건은 하나씩 물어봄 |

---

## 6. 자료 구조

```js
{
  version: 2,
  fiscalYear: 2026,               // 지금 보고 있는 해
  lastBackupAt: "2026-09-11...",
  years: {                        // 회계연도마다 장부를 따로 담는다
    "2026": {
      classes  : [ {id, name} ],
      children : [ {id, name, enterDate, leaveDate,
                    classHistory:[{classId, startDate}],   // 반 이동 이력
                    consignPeriods:[{startMonth, endMonth}] } ],  // 위탁 기간 (월 단위)
      items    : [ {id, name, group, targetMode, enabled, stdAmount, note} ],
      rosters  : [ {id, name, childIds:[]} ],     // 고정 명단
      vouchers : [ {id, date, voucherNo, itemId, summary, perAmount, total,
                    lines:[{childId, amount, memo}] } ],    // 수납결의서
      receipts : [ ... ],                                   // vouchers에서 파생 (읽기 전용)
      expenses : [ {id, date, voucherNo, itemId, summary,
                    total, needAmount, cardFee, operAmount, memo,
                    allocations:[{childId, amount}],        // ← 명단
                    overrides:[{childId, include, reason}] } ]
    },
    "2027": { ... }
  }
}
```

**핵심 두 가지**

- `expenses[].allocations` — 이것이 **명단**입니다. 이 프로그램의 존재 이유.
- `years` — 새 회계연도를 시작해도 지난 해가 **지워지지 않습니다.**
  화면 위 회계연도 드롭다운으로 오갑니다. 백업 복원이 아닙니다.

**계산의 중심 함수 두 개**

```js
allocate(amount, childIds)   // 1/n 배분 + 나머지는 앞에서부터 1원씩
balance(childId, itemId, from, to)
  → { paid, used, consign, totalUsed, left }
     left = paid − used        // used = 전체 사용 − 위탁 충당분
```

---

## 7. 개발 과정

### 7-1. 단계별로 쌓은 것

| 단계 | 내용 |
|---|---|
| 1단계 | 잔액이 실시간으로 나오는 상태 — 배분·잔액 계산, 검증 규칙 |
| 2단계 | 산출물 — 정산서·안내문·보고서·대장·인쇄·CSV |
| 3단계 | 위탁보육 — 월 단위 판정, 위탁 충당 열 |
| 4단계 | 회계연도 마감과 승계, 백업 알림 |
| 5단계 | 물품관리와 통합한 첫 화면 (`build.py`) |
| 6단계 | 연도별 자료 보관, 전체 조작 감사, 인쇄 정리 |

### 7-2. 실제 어린이집 자료를 보고 바로잡은 것

실무자가 찍어 보내 준 **총계정원장·개인별 대장·수납한도액 공고문** 사진을 보고 고친 것들입니다.
이 항목들이 이 프로그램을 종이 서식에 맞게 만들었습니다.

- **수납은 결의서 단위로** — 처음엔 원아별로 한 줄씩 넣게 만들었는데, 실무는 **수납결의서 1건**에 여러 원아가 붙는 형태였습니다. 서식에 맞춰 다시 만들었습니다.
- **대장은 아동 × 계정과목 × 월 12칸** — 세목마다 표를 따로 뽑던 것을 한 장에 합쳤습니다.
- **위탁보육은 월 단위** — 날짜 단위로 재원 구간을 나누던 것을 달 단위로 바꿨습니다. 위탁보육료는 그 달치를 그 달 아무 날에나 받기 때문입니다.
- **수납한도액 검사 삭제** — 「한도를 넘었다」는 경고를 없앴습니다. 어린이집은 한도를 지키고 있고, 중요한 것은 들어온 돈의 정산이기 때문입니다.
- **211목/221목에 목 이름 병기** — 숫자만으로는 실무자가 못 알아봅니다.
- **앨범비 추가** — 세목 목록에서 빠져 있었습니다.

### 7-3. 「보는 사람 입장에서」 걷어낸 것

실무자의 지적으로 **문구를 대거 덜어냈습니다.** 디자인에서도 같은 기준을 지켜 주세요.

- 출력물에서 **설명 문구·근거 인용·주의 안내를 전부 뺐습니다.**
  ("너무 AI slop 같다" — 보고서를 받아보는 사람에게 필요 없는 말은 넣지 않습니다.)
- 보호자가 받는 안내문에서 **지출결의서 번호**를 뺐습니다. 보호자가 알 이유가 없습니다.
- **작성일 · 원장 (인) · 보호자 (인) 칸을 모든 출력물에서 없앴습니다.**
- 인쇄할 때 **브라우저 머리글·바닥글(날짜·파일 경로)이 안 찍히는 것이 기본값**입니다. (`@page{margin:0}` + `body{padding:12mm 11mm}`)
- 1인당 부담금액을 여러 숫자로 쪼개 보여주던 것을 **큰 숫자 하나**로 바꿨습니다.
- **반환금 확정·기록 기능을 통째로 삭제**했습니다. 정산서까지가 이 프로그램의 일입니다.

### 7-4. 잡은 버그

| 증상 | 원인 |
|---|---|
| 반·원아 드롭다운이 안 먹음 | 고른 값을 임시 객체에 적어 두고 화면을 다시 그려서 잃어버림 |
| 정산 기간 마지막월이 되돌아감 | 시작월·마지막월을 앞뒤로 바꿔 잡는 코드 |
| 「연간」 버튼이 늘 파랗게 | 지금 기간과 상관없이 표시 |
| 인쇄하면 1쪽만 나옴 (통합본) | 화면틀이 화면 높이만큼만 잡혀서 → 인쇄 직전 내용 높이만큼 늘림 |
| 대장 글자가 세로로 쪼개짐 | A4에 12칸이 안 들어감 → `font-size:8px` + `white-space:nowrap`, 가로 3쪽 |
| `<h3>`+`<table>`을 함께 만들면 표가 사라짐 | HTML 조각 생성 함수가 첫 번째 요소만 돌려줌 |

### 7-5. 회귀 테스트 6벌 (Playwright)

```
node tests/flow.js       빈 상태에서 화면을 따라가며 처음부터 끝까지 입력
node tests/reports.js    심어 둔 자료로 대장·정산서·보고서·안내문 숫자 검산
node tests/dummy.js      예시자료.json(37명·7반)을 모든 화면에 통과
node tests/rollover.js   새 회계연도 승계 — 지난 해가 남는지, 2단계로 도는지
node tests/audit.js      모든 버튼·드롭다운이 제 몫을 하는지 + 업무 흐름 연결
node tests/launcher.js   통합 첫 화면 — 저장 공유·인쇄·백업·버전 표시
```

`tests/audit.js`는 처음엔 버튼을 실제로 눌러 보는 방식이었는데,
화면이 다시 그려지며 자리가 밀리고 「수정」 버튼이 입력칸을 고침 상태로
바꿔 놓아 **헛것이 잔뜩 잡혔습니다.** 지금은 **누르지 않고**
핸들러가 붙어 있는지만 확인하고, 업무 흐름은 따로 끝까지 따라갑니다.

---

## 8. 파일 구성

```
기타필요경비정산.html    212 KB   ← 정산 프로그램 원본. 여기를 고칩니다
물품관리시스템.html      169 KB   ← 완성품. 절대 고치지 않습니다
build.py                          ← 위 둘을 한 파일로 묶습니다
어린이집살림도우미.html  516 KB   ← 어린이집에 건네는 파일 (build.py가 만듦)
예시자료.json             90 KB   ← 시험용 더미 (37명·7반·온갖 상황 포함)
tests/                            ← 회귀 테스트 6벌
```

**고친 뒤에는 반드시 `python3 build.py`를 다시 돌려야** 합쳐진 파일에 반영됩니다.
합쳐진 파일을 직접 고치면 다음 빌드에서 덮어써집니다.

**물품관리시스템.html은 손대지 않습니다.** 통합본에서만 덜어내야 하는 것
(어린이집 이름 설정, 버전 표시)은 `build.py`의 `PATCHES`에서
**담을 때만** 바꿉니다. 원본 파일은 그대로입니다.

---

## 9. 디자인 작업자에게 — 바꿔도 되는 것과 안 되는 것

### 9-1. 마음껏 바꿔도 되는 것

- **색·간격·글꼴·모서리 둥글기** — `기타필요경비정산.html` 맨 위 `:root` 변수와 `<style>`(7~186줄)
  ```css
  --bg --panel --line --line2 --ink --dim --faint
  --accent --accent-bg --warn --warn-bg --bad --bad-bg --ok --ok-bg --radius
  ```
- **첫 화면 레이아웃** — `build.py`의 `PAGE` 문자열 안 `<style>`과 `#home` 마크업
- 탭 막대·버튼·표·카드의 **생김새**
- 여백, 그림자, 호버 효과, 아이콘

### 9-2. 바꾸면 깨지는 것

| 절대 바꾸지 말 것 | 이유 |
|---|---|
| **`id` 속성** (`#c_pick`, `#e_total`, `#bk_from`, `#nt_child` …) | 자바스크립트와 테스트가 id로 요소를 찾습니다 |
| **`data-` 속성** (`data-tab`, `data-m`, `data-p`, `data-a`) | 탭·화면·기간·동작을 가리킵니다 |
| **`.no-print` / `.print-only` / `.printhead` / `.sheet` / `.pagebreak`** | 인쇄 때 무엇을 빼고 어디서 쪽을 넘길지 정합니다 |
| **`@media print` 블록** | 특히 `@page{margin:0}`(머리글·바닥글 억제), `thead{display:table-header-group}`(쪽마다 머리행 반복), `tr{break-inside:avoid}`, `table.ledger` 축소 |
| **`.pri` 클래스** | 지금 고른 기간·화면을 파랗게 표시하는 표시자. 테스트가 이걸로 확인합니다 |
| **자바스크립트 (217줄 이후)** | 계산·저장·출력 전부. 디자인 작업에서 건드릴 일이 없습니다 |
| **물품관리시스템.html** | 완성품입니다 |

### 9-3. 문구를 손볼 때의 기준

1. **보고서·정산서·안내문에는 설명을 넣지 않습니다.** 받아보는 사람이 읽을 이유가 없는 문장은 전부 뺍니다.
2. **화면 안내(`.hint`, `.note`, `.law`)는 인쇄되지 않습니다.** 그러니 화면용 설명은 여기에 넣으면 됩니다.
3. 대상 독자는 **컴퓨터를 잘 다루지 못하는 어린이집 사무원**입니다. 전문용어보다 「무엇을 누르면 무엇이 된다」로 씁니다.

### 9-4. 손본 뒤 확인

```bash
python3 build.py                 # 합쳐진 파일 다시 만들기
node tests/audit.js              # 버튼·드롭다운이 살아 있는지
node tests/launcher.js           # 첫 화면과 통합이 멀쩡한지
```

인쇄 화면(Ctrl+P 미리보기)은 **대장**과 **반환 정산서(전 원아)** 두 개를 꼭 확인하세요.
가장 길고 가장 잘 깨지는 표입니다.

---

## 10. 남은 것 / 앞으로

- 실제 어린이집에 배포해 써 보고 받는 피드백에 따라 고쳐 나갈 계획입니다.
- 의견 주실 곳 : **hjlee9446@korea.kr**
  기능 제안·고칠 점은 물론, **개정본을 계속 받아보고 싶은 어린이집**도 같은 주소로 연락 주시면 됩니다.
