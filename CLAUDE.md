# 어린이집 살림도우미 — 작업 규칙

## 절대 깨뜨리면 안 되는 것 — 이미 뿌린 백업 파일

**2026-09-18, 첫 어린이집에 프로그램을 건넸다.** 그 어린이집은 그 판으로 자료를
쌓는다. 앞으로 무엇을 고치든 **그 백업 파일은 반드시 열려야 하고, 열었을 때
숫자가 한 원도 달라져서는 안 된다.** 이미 보호자에게 나눠 준 정산서와 어긋나기
때문이다. 물품관리도 마찬가지로, 감사를 나간 어린이집들이 이미 그 판으로
물품을 등록해 쓰고 있다.

그래서 배포한 판과 그 판으로 만든 백업을 저장소에 붙박아 두었다.

```
tests/fixtures/기타필요경비정산_배포본.html        2026-09-18 건넨 정산 프로그램 (v1.0.0)
tests/fixtures/정산_배포본_백업.json               그 판으로 뽑은 백업
tests/fixtures/물품관리시스템_배포본.html          어린이집에 이미 뿌린 물품관리
tests/fixtures/기타필요경비정산_배포본_v1.0.3.html  2026-09-22 건넨 판 (2차 배포)
tests/fixtures/어린이집살림도우미_v1.0.3.html       그날 건넨 통합본 그대로
```

**이 파일들은 고치지 않는다.** 기준점이기 때문이다.
새 판을 배포할 때마다 그 판을 여기에 한 벌 붙박아 둔다.

`tests/compat-settle.js`는 배포판과 지금 판을 **나란히 띄워** 같은 백업을 넣고
숫자를 맞춰 본다. 기대값을 시험에 적어 두지 않으므로 「기대값을 고쳐 통과시키는」
일이 생기지 않는다.

### 저장 구조를 고칠 때

| 해도 되는 것 | 하면 안 되는 것 |
|---|---|
| 칸을 **더한다** (없으면 기본값으로 채운다) | 있던 칸의 **이름을 바꾼다** |
| 낡은 칸을 **읽어서 새 칸으로 옮긴다** (`fixYear`) | 있던 칸을 **말없이 버린다** |
| 화면·문구·표시 방법을 바꾼다 | 같은 자료에서 **다른 숫자**가 나오게 한다 |

칸을 옮겨야 하면 `fixYear()`에서 옛 값을 읽어 새 자리에 넣고 그 뒤에 지운다.
`cardFee`를 지출에서 수납으로 옮길 때 옛 값을 운영비 부담분에 합친 것이 그 예다.

## 파일

```
기타필요경비정산.html    정산 프로그램 원본 — 여기를 고친다
물품관리시스템.html      물품관리 원본
build.py                 위 둘을 묶어 어린이집살림도우미.html을 만든다
어린이집살림도우미.html  어린이집에 건네는 파일 (build.py가 만듦, 직접 고치지 말 것)
예시자료.json            시험용 더미
개발문서.md              개발 목적·설계 원칙·디자인 인수인계
```

**고친 뒤에는 반드시 `python3 build.py`를 다시 돌린다.**

통합본에서만 덜어낼 것(물품관리의 어린이집 이름 설정·버전 표시)은 `build.py`의
`PATCHES`에서 담을 때만 바꾼다. 고칠 자리를 못 찾으면 빌드가 멈춘다.

## 시험 — 고치면 전부 돌린다

```bash
python3 build.py
for t in flow reports dummy rollover audit launcher compat compat-settle items refund audit2 split; do
  node tests/$t.js || echo "$t 실패"
done
```

| 시험 | 무엇을 보나 |
|---|---|
| `flow` | 빈 상태에서 화면을 따라가며 처음부터 끝까지 입력 |
| `reports` | 대장·정산서·보고서·안내문 숫자 검산 |
| `dummy` | 예시자료를 모든 화면에 통과 |
| `rollover` | 새 회계연도 승계 — 지난 해가 남는지 |
| `audit` | 모든 버튼·드롭다운이 제 몫을 하는지 + 업무 흐름 연결 |
| `launcher` | 통합 첫 화면 — 저장 공유·인쇄·백업 |
| `compat` | 이미 뿌린 물품관리시스템의 백업이 들어오는지 |
| `compat-settle` | **배포한 정산 프로그램의 백업이 숫자 그대로 열리는지** |
| `items` | 세목이 모든 화면·서류에 따라붙는지 |
| `refund` | 「반환 완료로 표시」·「반환 완료 취소」가 모든 표에 따라붙는지 |
| `audit2` | **외부 점검 26건(1차 15 · 2차 11)이 되살아나지 않는지** |
| `split` | 「원아별로 나누기」 — 잔액까지만 담고 남는 몫이 운영비로 넘어가는지 |

Playwright는 `NODE_PATH`에 설치해 두고 쓴다. 브라우저는
`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`.

## 이 프로그램의 성격

어린이집 실무자(컴퓨터를 잘 다루지 못하는 사무원)가 쓰는 도구다. 감사 검증
도구가 아니라 **정산을 쉽게 하도록 돕는 도구**다.

- 보고서·정산서·안내문에는 **설명을 넣지 않는다.** 받아보는 사람이 읽을 이유가
  없는 문장은 뺀다. 다만 표를 읽는 데 꼭 필요한 설명은 `.note.show-print`로
  인쇄물에도 내보낸다.
- **세목 간 상계를 하지 않는다** (재무회계규칙 제15조).
- **반은 통계 단위가 아니다.** 원아를 빠르게 고르기 위한 도구일 뿐이다.
- 반환할 금액을 계산해 **정산서를 뽑고, 돌려줬다는 사실을 적어 두는 데까지**가 이
  프로그램의 일이다. 이체도 전표도 대신하지 않는다. 다만 적어 두지 않으면 이미
  돌려준 돈을 또 「돌려줄 돈」으로 잡으므로, `refunds`에 **반환일·원아·세목·금액**만
  남긴다 (「가」 방식). 표시·취소는 언제든 되돌릴 수 있어야 한다.
- 자료는 이 컴퓨터 밖으로 나가지 않는다 (localStorage + JSON 백업).

## 판올림

`build.py`의 `VERSION`과 `PATCHES_LOG`를 함께 올린다. 첫 화면 우측 하단
**「📋 패치노트」** 버튼이 그 기록을 그대로 보여 준다. 어린이집이 「무엇이
달라졌나」를 보는 자리이므로 **한 줄에 하나씩, 짧게** 적는다.
시험은 버전을 박아 두지 않고 `build.py`에서 읽어 오므로 판올림해도 고칠 것이 없다.

## 지켜야 할 것 — 외부 점검 26건에서 나온 규칙

1차(09-21) 15건, 2차(09-22) 11건. 전부 고쳤고 `tests/audit2.js`에 못 박았다.
**새 기능을 붙이거나 값을 고칠 때 이 목록을 먼저 훑는다.**

### 회계 제도가 정한 것 — 프로그램이 여기에 맞춰야 한다

- **지출결의서는 달이 지나면 확정된다.** 시에 매월 회계보고를 하므로 **전월 건은 고칠 수 없다.**
  그래서 「연말에 모아서 정리」하는 방식은 쓸 수 없다. 갈라야 할 것은 **그 달에 등록할 때**
  갈라 두어야 한다. `openAllocModal()`이 그 자리다.
- **모자란 몫을 균등배분 안에서 처리할 수 없다.** 모자람은 원아마다 다른데 운영비 부담분은
  지출건에 한 칸뿐이고, 필요경비를 줄이면 1/n이 다시 바뀌어 **순환한다**(실제로 4회를 돌려도
  0이 되지 않았다). 유일한 해법은 **원아별로 금액을 달리 담는 것**이다.
- **수납이 덜 된 아이는 운영비로 메울 일이 아니다.** 잔액이 마이너스면 미수금이므로
  「잔액까지만 나누기」가 건드리지 않는다.
- **3분기까지는 초과가 용인된다.** 다음 분기 수납으로 메워지기 때문이다. 4분기부터만 알린다.
  다만 「원아별로 나누기」 자체는 중간입소·위탁 전환 때문에 **늘 열어 둔다**.

### 돈에 관한 것

- **금액 기록이 가리키는 원아·세목은 절대 사라지면 안 된다.** 승계가 명부만
  덮어써서 그 해 신입 원아의 10만원이 반환 대상에서 통째로 빠졌다(R01).
  `keepReferenced()`가 되살리고, 검증이 「명부에 없는 원아의 금액 기록」으로 잡는다.
- **화면의 체크와 계산의 기준이 같아야 한다.** 「포함」에 체크가 켜져 있는데
  사용액이 0원이었다(R02). 기준일 이후 지출은 `overrides`가, 기간 내 지출은
  `allocations`가 기준이다. 체크의 뜻도 날짜에 따라 다르게 해야 맞는다.
- **미확정 금액을 확정 금액처럼 저장하지 않는다.** 협의 전 입학준비금(R03),
  음수(R04), 「한글」→0원(R05). 「협의해서 0원」과 「아직 협의 안 함」은 다르다.
- **검사는 화면이 아니라 저장하는 자리에 붙인다.** 화면 한 곳만 막으면 다른
  길로 또 들어온다. `markRefund()`가 음수를 한 번 더 막는 이유다.
- **잘못 적은 값을 0원으로 바꾸지 않는다.** 0원은 정상적인 회계 값이라
  아무도 이상하게 여기지 않는다. `moneyInput()`이 원래 값을 되돌린다.

### 이름과 범위에 관한 것

- **한 이름은 한 값에만 붙인다.** 「장부 차액」이 두 값에 붙어 4,986원 차이가
  났다(R06). 장부 차액 · 돌려줄 돈 · 메워야 할 돈 · 협의 전 · 협의로 줄인 몫은
  전부 다른 값이고, 다른 것이 정상이다. **같게 만들려고 원장을 고치면 안 된다.**
- **차감 범위와 조회 범위는 다르다.** 반환액을 뺄 때는 회계연도 누적이 맞지만,
  「이 기간에 얼마 지급했나」를 보여 줄 때는 기간이 맞다. 한 함수로 처리해
  반기 보고서의 반환 열이 연간 누계가 됐다(5-1). 지금은 「연간 누계」라고
  이름에 밝히고 반기끼리 더하지 말라고 적는다.
- **합계를 한 날짜에 붙이지 않는다.** 두 번 나눠 돌려줬는데 한 날짜에 40,000원을
  준 것처럼 적혀 622목 지출결의와 대조가 깨졌다(R07).
- **화면 · CSV · 인쇄물은 같은 기준이어야 한다.** CSV만 순액·보호자부담을 써서
  같은 서류가 두 숫자로 읽혔다(R08).

### 기능을 끌 때

- **끄는 것은 「앞으로 안 쓴다」는 뜻이지 「지난 기록을 못 고친다」가 아니다.**
  세목을 전부 끄면 예전 결의서 수정 화면에 들어가는 길까지 막혔다(R09).
  비활성 세목이라도 그 거래가 쓰던 것은 목록에 남긴다(`itemsForEdit`).

### 설명서

- **없는 기능을 안내하지 않는다.** FAQ가 「사유만 고르면 갈라 준다」고 적었는데
  그런 기능이 없었다(R10). 기능을 고치면 설명서도 같이 고친다.

### 고칠 때의 절차

한 값을 고치면 **그 값을 쓰는 화면을 전부 찾는다.** 반환액 하나가 반환 정산서 ·
운영위 보고서 · 보호자 안내문 · 검증 · 원아별 현황 · CSV 여섯 군데에 나온다.
1차에서 안내문을 빠뜨렸고, 2차에서 CSV를 빠뜨렸다.

**한 번에 적게 고친다.** 15건을 하루에 고쳤더니 5건이 새로 생겼다.

### 1차에서 배운 것

- **저장 칸을 더하면 그 칸을 만드는 자리를 전부 찾아라.** `refunds`를 더할 때
  `emptyYear`·`fixYear`는 고쳤는데 승계 마법사의 손으로 지은 객체를 빠뜨려,
  새 회계연도에서 반환 버튼이 오류로 죽었다. 지금은 `emptyYear()`를 바탕으로
  깔고 덮어쓴다.
- **반환은 「그 기간의 정산」이 아니라 「쌓인 잔액의 청산」이다.** 날짜로 자르면
  이월에 되살아나고, 기간 밖 날짜로 몇 번이고 다시 찍을 수 있다.
- **고칠 때의 세목 목록은 새로 적을 때와 다르다.** 사용을 끈 세목이라도 그
  거래가 쓰던 세목은 목록에 남겨야 한다. 아니면 적요만 고쳐도 귀속이 바뀐다.
- **원칙은 화면 하나가 아니라 길목마다 지켜야 한다.** 「1원이라도 어긋나면
  저장하지 않는다」가 지출 등록에만 있어, 정산 문답으로 마지막 원아를 빼면
  배분 합계가 0원이 됐다.
- **세목 간 상계 금지는 검증 화면에도 적용된다.** 수입−지출인 「장부 차액」은
  반환액이 아니다. 돌려줄 돈은 세목별 양수 잔액의 합이다.
- **친절해 보이는 선택지가 해석 불가능한 자료를 만든다.** 카드수수료 「방식만
  바꾸고 금액은 그대로」가 그랬다. 결의서마다 방식을 적어 두지 않으므로 없앴다.

자세한 것은 `개발문서.md`.
